# Núcleo — imprescindible
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
openpyxl>=3.1

# Informes en Word con tus propias plantillas
python-docx>=1.1
docxtpl>=0.18
jinja2>=3.1

# Inteligencia artificial (escáner de etiquetas, albaranes y asistente)
anthropic>=0.69

# Código QR en la consola para conectar el móvil (opcional)
qrcode>=8.0
